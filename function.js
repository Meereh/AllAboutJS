//함수 선언식 (function declarations)
function add(a, b) {
    return a + b;
}

//함수 표현식 (functin Expressions)
const minus = function (x, y) {
    return x - y;
}


//화살표 함수(arrow function)
const multiple = (x, y) => { x * y }

